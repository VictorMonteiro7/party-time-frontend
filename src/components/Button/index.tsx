import styled from "styled-components";

export const Button = styled.button`
  background: var(--purple);
  color: var(--white);
  border: none;
  padding: 16px 24px;
  font-size: 1rem;
  font-weight: bold;
  cursor: pointer;
  outline: none;
  border-radius: 5px;
`;
